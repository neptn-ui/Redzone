# backend/requirements.txt
# Pin major versions. Exact pins applied after integration testing.
# =============================================================

# --- Web framework -------------------------------------------
fastapi==0.111.*
uvicorn[standard]==0.30.*
python-multipart==0.0.*        # for form data / file uploads
python-dotenv==1.0.*

# --- Database / GIS ------------------------------------------
sqlalchemy==2.0.*
geoalchemy2==0.15.*
psycopg2==2.9.*                # C extension — needs libpq-dev
alembic==1.13.*                # schema migrations

# --- Geospatial ----------------------------------------------
geopandas==0.14.*
shapely==2.0.*
pyproj==3.6.*

# --- Scheduling (live signal pollers) ------------------------
apscheduler==3.10.*

# --- Satellite / remote sensing ------------------------------
sentinelhub==3.10.*

# --- Optimization (assignment solver) ------------------------
scipy==1.13.*
pulp==2.8.*

# --- ML / CV (secondary confidence layer + change detection) -
scikit-learn==1.5.*
xgboost==2.0.*
opencv-python-headless==4.10.*
numpy==1.26.*
pandas==2.2.*

# --- HTTP clients (API calls + caching) ----------------------
httpx==0.27.*
aiohttp==3.9.*

# --- LLM (narrative generation) ------------------------------
anthropic==0.29.*

# --- Testing -------------------------------------------------
pytest==8.2.*
pytest-asyncio==0.23.*
httpx                          # already listed; used in test client

# --- Utilities -----------------------------------------------
pydantic==2.7.*
pydantic-settings==2.3.*
tenacity==8.3.*                # retry logic for flaky external APIs
